# 📦 Python Learning Progress – Rajender

---

## ✅ Day 01 — Basics

### ✅ Variables & Types

- Declared variables dynamically (no type keywords like Dart)
- Used `type()` to check variable types
- Practiced basic types: `int`, `float`, `str`, `bool`

### ✅ Printing

- Used `print()` for values, types, debugging
- Combined strings and variables using commas and f-strings

---

## ✅ Day 02 — Collections & Functions

### ✅ Lists

- Created lists: `[]`
- Added items with `.append()`
- Removed with `.remove()` / `del`
- Looping using `for item in list`, `for i in range(len(list))`
- Wrote functions: `sum_list()`, `find_max()`

### ✅ Tuples

- Immutable ordered collections
- Used for fixed data
- Unpacking values

### ✅ Sets

- Unordered unique collections
- Used `.add()`, `.remove()`, `in`, `not in`
- Auto-removes duplicates

### ✅ Dictionaries

- Key-value mappings: `{ "name": "Raj" }`
- Accessed via `[]` and `.get()`
- Updated values and added new keys
- Looped with `.items()`

---

## ✅ Day 03 — Loops & Conditionals

### ✅ Conditionals

- `if`, `elif`, `else` blocks
- Logical operators: `and`, `or`, `not`

### ✅ Loops

- `while True:` for infinite loops with break conditions
- `for` loops over ranges and collections

### ✅ Mini Tasks

- Built calculators
- Menu-driven apps using `while`

---

## ✅ Day 04–06 — Mini Practice Projects

### ✅ Practiced:

- Menu-driven logic
- Arithmetic apps
- Type checking
- Validation using `isinstance()`
- Used `input()` and `int()` conversions

---

## ✅ Day 07 — Functions & Parameters

- Wrote reusable functions with parameters
- Returned values using `return`
- Nested functions and modular logic

---

## ✅ Day 08 — Profile Manager CLI Project

### ✅ Built an interactive menu app

- Managed user profile data in a dictionary
- Handled list fields like skills
- Checked types
- Input validation
- Looped menu with exit option

---

## ✅ Day 09 — File Handling

### ✅ Text File I/O

- Read files using `'r'`
- Write using `'w'`, append using `'a'`
- Used `with open(...)` context manager
- `.read()`, `.readlines()`, `.strip()`
- Wrote profile data to file

---

## ✅ Day 10 — Modularization & Imports

### ✅ Created custom modules

- Separated logic into files: `main.py`, `tasks.py`, `contacts.py`
- Imported with `from module import function`
- Used `__main__` guard

### ✅ Used built-in modules

- `random`, `datetime`, `math`, `os`, `json`
- Understood reusable, clean code via modularization

---

## ✅ Day 11 — Error Handling

### ✅ Try/Except

- Prevented crashes from invalid input
- Used `try/except/else/finally`

### ✅ Custom Exceptions (Intro)

- Used `raise` keyword
- Learned structure of custom exception classes (to revisit with OOP)

---

## ✅ Day 12 — JSON & Data Persistence

### ✅ `json` module

- Used `json.dump()` and `json.load()`
- Stored structured data (lists, dicts) in `.json` files
- Separated file for each module: tasks.json, contacts.json

### ✅ Smart Life Manager CLI Project (Phase 2)

- Managed:
  - Tasks: Add, List, Mark Complete, Delete
  - Contacts: Add, List, Delete
  - Expenses: Add, View, Total, By Category
- Used JSON files for persistent data
- Maintained menu navigation system

---

## ✅ Day 13 — Object-Oriented Programming (OOP)

### ✅ Classes & Objects

- Created classes using `class` keyword
- Defined `__init__()` constructor
- Used `self` to access instance attributes
- Created methods inside classes

### ✅ Encapsulation

- Organized behavior + data inside objects
- Accessed and updated instance variables
- Modeled real-world entities (`Task`, `Contact`, etc.)

### ✅ Applied in Project (In Progress)

- Migrating functions to class-based system
- Structuring app using OOP principles

---

## ✅ Concepts & Tools Covered So Far

- ✅ Variables, Types, Input/Output
- ✅ Lists, Tuples, Sets, Dictionaries
- ✅ Loops (`for`, `while`)
- ✅ Conditionals (`if`, `elif`, `else`)
- ✅ Functions & Return Values
- ✅ File I/O (`txt` and `json`)
- ✅ Modular Programming (`import`, reusable files)
- ✅ Error Handling (`try/except`, `raise`)
- ✅ Object-Oriented Programming (`class`, `object`, `init`, `self`)
- ✅ CLI Project: Smart Life Manager (modular + file I/O + menus)

---

## 🔜 Coming Up Next

- ✅ OOP: Inheritance, Polymorphism
- 🟩 CSV, Excel file handling
- 🟩 `argparse` for CLI arguments
- 🟩 `requests` module for API calls
- 🟩 SQLite database (optional)
- 🟩 Building real CLI tools

---

🎯 **Rajender is now entering Intermediate Level Python.**
